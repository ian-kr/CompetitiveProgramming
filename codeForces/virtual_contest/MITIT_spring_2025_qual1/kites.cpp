#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main(void){

    int tt;
    cin >> tt;

    while(tt--){
        ll n;
        cin >> n;
        vector<ll> sticks(n);
        for(auto i&: sticks) cout << i;
        sort(sticks.begin(),sticks.end());
        vector<ll> prefix_sum
    }


    return 0;
}